spring:
  datasource:
    url: jdbc:mysql://172.18.0.2:3306/testdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Taipei
    username: root
    password: root+123

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  config: classpath:logback-spring-prod.xml
  level:
    com.svm.backend: info
