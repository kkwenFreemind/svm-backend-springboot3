server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/testdb?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Taipei
    username: root
    password: root+123
  jpa:
    properties:
      dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
      show-sql: true


logging:
  config: classpath:logback-spring.xml
  level:
    com.svm.backend: trace

jwt:
  app:
    jwtSecret: ======================BezKoder=Spring===========================
    jwtExpirationMs: 86400000
    tokenHead: 'Bearer '

secure:
  ignored:
    urls: #url白名單
      - /api/auth/signing
      - /api/auth/signup
      - /api/test/
      - /swagger-ui/index.html
      - /actuator/health
      - /actuator/prometheus
      - /admin/login
